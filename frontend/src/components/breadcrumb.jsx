import React from "react";

const breadcrumb = ({ iconos }) => {
  return (
    <div>
      {/* voy a gregar un breadrumbs donde podemos cmabiar el icono donde le responde  */}
    </div>
  );
};

export default breadcrumb;
